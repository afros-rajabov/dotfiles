return {
  "shortcuts/no-neck-pain.nvim",
  opts = {
    width = 130,
    -- disableOnLastBuffer = true,
    autocmds = {
      -- "safe"
      -- enableOnVimEnter = false
    },
    mappings = {
      enabled = true
    },
    -- integrations = {
    --   dashboard = {
    --     enabled = true
    --   }
    -- }
  }
}
